{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
//#if (UseMySql)
    // 如果需要连接自己的MySQL环境，可以在这里配置连接字符串
    // If you need to connect to your own MySQL environment, you can configure the connection string here
    "MySql": "Server=localhost;User ID=root;Password=123456;Database=dev;SslMode=none",
//#elif (UseSqlServer)
    // 如果需要连接自己的SQL Server环境，可以在这里配置连接字符串
    // If you need to connect to your own SQL Server environment, you can configure the connection string here
    "SqlServer": "Server=localhost;Database=dev;User ID=sa;Password=Test123456!;TrustServerCertificate=true",
//#elif (UsePostgreSQL)
    // 如果需要连接自己的PostgreSQL环境，可以在这里配置连接字符串
    // If you need to connect to your own PostgreSQL environment, you can configure the connection string here
    "PostgreSQL": "Host=localhost;Database=dev;Username=postgres;Password=123456",
//#elif (UseSqlite)
    "Sqlite": "Data Source=demo.db",
//#endif
    "Redis": "localhost:6379"
  },
//#if (UseRabbitMQ)
  "RabbitMQ": {
    "HostName": "localhost",
    "UserName": "guest",
    "Password": "guest",
    "VirtualHost": "/",
    "Port": 5672
  },
//#elif (UseKafka)
  "Kafka": {
    "Servers": "localhost:9092"
  },
//#elif (UseAzureServiceBus)
  "AzureServiceBus": {
    "ConnectionString": "Endpoint=sb://your-servicebus.servicebus.windows.net/;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=your-key"
  },
//#elif (UseAmazonSQS)
  "AmazonSQS": {
    "Region": "us-east-1",
    "AccessKey": "your-access-key",
    "SecretKey": "your-secret-key"
  },
//#elif (UseNATS)
  "NATS": {
    "Servers": "nats://localhost:4222"
  },
//#elif (UsePulsar)
  "Pulsar": {
    "ServiceUrl": "pulsar://localhost:6650"
  },
//#endif
  "Services": {
    "user": {
      "https": [
        "https://user:8443"
      ]
    },
    "user-v2": {
      "https": [
        "https://user-v2:8443"
      ]
    }
  }
}
